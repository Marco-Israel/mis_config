TEST
"3.2.2.7_Extension_CRLDistributionPoints"
--------------------------------------------------------------------------------
REQUIRED SCOPE:

1.
The one and only DistributionPoint [...] never the attribute reasons

2.
The fullName [...] exactly one GeneralName; the CHOICE type [...]
is always registeredID; the OID signals the transport [...]

3.
The crlIssuer is a SEQUENCE with exactly one GeneralName; the CHOICE type for
this single element is always directoryName; the directoryName

--------------------------------------------------------------------------------
TESTLIST:

1_requirements_oposide__noFullName_multichoice_withReason_scrlIssuerOtherName
- Uses multi distribution Points
- no fullName used but exactly required
- The crlIssuer uses otherName, not a DirName
- Reasons is used (instead of fullname)


--------------------------------------------------------------------------------
1_requirements_oposide__noFullName_multichoice_withReason_scrlIssuerOtherName

EXPACT:
Because all settings given in the configuration are according to the standard
the certificates will be generated without errors

RESULT
TRUE


--- EOF ------------------------------------------------------------------------
